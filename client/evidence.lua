-- Bag/tag evidence, RedM entity markers, chain-of-custody

function CollectEvidence(coords, description)
    -- Spawn evidence marker at coords using RedM natives
    -- Open evidence UI, link to MDT report
end

RegisterNetEvent("lxr-police:evidence:collect")
RegisterNetEvent("lxr-police:evidence:lockerAccess")